---
import { getCollection } from "astro:content";

// Obtener todos los artículos
const articulos = await getCollection("articulos");

// Ordenarlos por fecha descendente
const lista = articulos.sort(
  (a, b) => new Date(b.data.pubDate || b.data.date) - new Date(a.data.pubDate || a.data.date)
);
---

<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description"
    content="Lista de artículos sobre sostenibilidad tecnológica y proyectos forestales en Raíz Verde.">
  <title>Artículos - Raíz Verde</title>
  <link rel="canonical" href="https://raiz-verde-astro.vercel.app/articulos/">

  <!-- Critical CSS inline -->
  <style>
    body {
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.8;
      background: #121212;
      color: #e0e0e0;
      padding: 3rem 1rem;
      max-width: 860px;
      margin: 0 auto;
      font-size: 1.15rem;
    }

    header {
      text-align: center;
      margin-bottom: 3rem;
      padding-bottom: 2rem;
      border-bottom: 1px solid #2d6a4f;
    }

    .logo {
      width: 130px;
      height: 100px;
      margin-bottom: 1rem;
    }

    h1 {
      font-size: 3.2rem;
      color: #95d5b2;
      margin-bottom: .5rem;
    }

    .articles-index {
      border-bottom: 1px solid #2d6a4f;
      padding-bottom: 1.2rem;
      margin-top: -2rem;
    }

    a {
      color: #95d5b2;
      text-decoration: none;
    }

    article {
      margin-bottom: 2.5rem;
    }

    .meta {
      color: #74c69d;
      font-style: italic;
      margin: 0.5rem 0 1rem 0;
    }
  </style>

  <link rel="preload" href="/assets/css/style.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
  <noscript><link rel="stylesheet" href="/assets/css/style.css"></noscript>
  <link rel="icon" href="/assets/img/logo.svg" type="image/svg+xml">
</head>

<body>
  <header>
    <img src="/assets/img/logo.svg" alt="Logo Raíz Verde" class="logo">
    <h1>Raíz Verde</h1>
  </header>

  <main>
    <h2 class="articles-index">Todos los artículos.</h2>

    {lista.map((item, index) => (
      <article class={index === 0 ? "first" : ""}>
        <h2>
          <a href={`/articulos/${item.slug}`}>{item.data.title}</a>
        </h2>
        <div class="meta">
          {new Date(item.data.pubDate || item.data.date).toLocaleDateString("es-ES", {
            year: "numeric",
            month: "long",
            day: "numeric"
          })}
        </div>
        <p>{item.data.description}</p>
      </article>
    ))}

    <p><a href="/">← Volver al inicio</a></p>
  </main>

  <footer>
    <p>
      <a href="/rss.xml" target="_blank" rel="noopener noreferrer" style="margin-right: 1rem;">📡 RSS</a>
      Hecho con ❤️ y principios verdes
    </p>
    <p>© 2025 Raíz Verde — Hecho con principios sostenibles</p>
    <p class="carbon" style="font-size: 0.9rem; margin-top: 2rem;">
      Esta página está diseñada para tener una huella de carbono mínima:<br>
      <strong>Menos de 0.01g CO₂ por visita — Grado A</strong><br>
      Más limpio que el 99% de las webs testeadas.<br>
      <a href="https://www.websitecarbon.com/website/raiz-verde-six-vercel-app/" target="_blank" rel="noopener noreferrer">
        Ver cálculo completo en websitecarbon.com
      </a>
    </p>
  </footer>

</body>

</html>
