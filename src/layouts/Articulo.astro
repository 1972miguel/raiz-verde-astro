---
interface Props {
  articulo: any;
}

import Analytics from "@vercel/analytics/astro";

const { articulo } = Astro.props;
const data = articulo.data;
const pubDate = new Date(data.pubDate || data.date);
const formattedDate = pubDate.toLocaleDateString("es-ES", {
  year: "numeric",
  month: "long",
  day: "numeric",
});
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes" />

  <!-- SEO dinámico -->
  <title>{data.title} - Raíz Verde</title>
  <meta name="description" content={data.description} />

  <meta name="author" content={data.author || "MigLai"} />
  <meta name="keywords" content={(data.tags || []).join(", ")} />

  <!-- Open Graph -->
  <meta property="og:type" content="article" />
  <meta property="og:title" content={data.ogTitle || data.title} />
  <meta property="og:description" content={data.ogDescription || data.description} />
  <meta property="og:image" content={data.image || "/assets/img/logo.svg"} />
  <meta property="og:url" content={`https://raiz-verde-astro.vercel.app/articulos/${articulo.slug}`} />
  <meta property="article:published_time" content={pubDate.toISOString().split("T")[0]} />

  <!-- Twitter -->
  <meta name="twitter:card" content="summary" />
  <meta name="twitter:title" content={data.title} />
  <meta name="twitter:description" content={data.description} />
  <meta name="twitter:image" content={data.image || "/assets/img/logo.svg"} />
  <meta name="twitter:url" content={`https://raiz-verde-astro.vercel.app/articulos/${articulo.slug}`} />

  <!-- Canonical -->
  <link rel="canonical" href={`https://raiz-verde-astro.vercel.app/articulos/${articulo.slug}`} />

  <!-- CSS crítico inline -->
  <style>
    body {
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.8;
      background: #121212;
      color: #e0e0e0;
      padding: 3rem 1rem;
      max-width: 860px;
      margin: 0 auto;
      font-size: 1.15rem;
    }

    header {
      text-align: center;
      margin-bottom: 3rem;
      padding-bottom: 2rem;
      border-bottom: 1px solid #2d6a4f;
    }

    .logo {
      width: 130px;
      height: 100px;
      margin-bottom: 1rem;
    }

    h1 {
      font-size: 3.2rem;
      color: #95d5b2;
      margin-bottom: .5rem;
    }

    a {
      color: #95d5b2;
      text-decoration: none;
    }

    .breadcrumb {
      font-size: 0.9rem;
      color: #74c69d;
      margin: 2rem 0;
    }

    .breadcrumb a {
      color: #74c69d;
    }

    .meta {
      color: #74c69d;
      font-style: italic;
      margin: 1.5rem 0;
    }

    .content h3 {
      color: #95d5b2;
      margin-top: 2.5rem;
      font-size: 1.8rem;
    }

    .content p {
      margin-bottom: 1.2rem;
    }

    footer {
      margin-top: 4rem;
      text-align: center;
      opacity: 0.9;
    }
  </style>

  <!-- Estilos adicionales -->
  <link rel="preload" href="/assets/css/style.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
  <noscript><link rel="stylesheet" href="/assets/css/style.css"></noscript>

  <link rel="icon" href="/assets/img/logo.svg" type="image/svg+xml" />
  <Analytics />
</head>

<body>

  <a href="#main-content" class="skip-navigation">Saltar al contenido principal</a>

  <header role="banner">
    <a href="/">
      <img src="/assets/img/logo.svg" alt="Logo Raíz Verde" class="logo" width="130" height="100" />
    </a>
    <h1><a href="/">Raíz Verde</a></h1>
  </header>

  <main id="main-content" role="main">

    <!-- Breadcrumb -->
    <nav class="breadcrumb" aria-label="Ruta de Navegación">
      <a href="/">Inicio</a>
      <span aria-hidden="true"> / </span>
      <a href="/articulos">Artículos</a>
      <span aria-hidden="true"> / </span>
      <span aria-current="page">{data.title}</span>
    </nav>

    <article>
      <h2>{data.title}</h2>
      <div class="meta">{formattedDate}</div>

      <div class="content">
        <slot />
      </div>
    </article>

    <p><a href="/">← Volver al inicio</a></p>

  </main>

  <footer role="contentinfo">
    <nav aria-label="Navegación del pie de página">
      <a href="/">Inicio</a> •
      <a href="/about">Sobre mí</a> •
      <a href="/articulos">Artículos</a>
    </nav>

    <p>
      <a href="/rss.xml" target="_blank" rel="noopener noreferrer" style="margin-right: 1rem;">📡 RSS</a>
      Hecho con ❤️ y principios verdes
    </p>

    <p>© 2025 Raíz Verde — Hecho con principios sostenibles</p>

    <p class="carbon" style="font-size: 0.9rem; margin-top: 2rem;">
      Esta página está diseñada para tener una huella de carbono mínima:<br>
      <strong>Menos de 0.01g CO₂ por visita — Grado A</strong><br>
      Más limpio que el 99% de las webs testeadas.<br>
<a href="https://www.websitecarbon.com/website/raiz-verde-astro.vercel.app/" target="_blank" rel="noopener noreferrer">
  Ver cálculo completo en websitecarbon.com
</a>

    </p>
  </footer>

</body>
</html>
